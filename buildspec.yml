version: 0.2

phases:
  build:
    commands:
      - dotnet restore
      - dotnet build
  post_build:
    commands:
      - dotnet test ./core-web-app-test/core-web-app-test.csproj
artifacts:
  files:
    - ./core-web-app/bin/Debug/netcoreapp2.1/*